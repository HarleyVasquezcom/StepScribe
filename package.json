{
  "name": "stepscribe",
  "version": "1.0.0",
  "private": true,
  "description": "StepScribe — records page interactions (MV3): click/type/toggle steps with mini element captures (drawImage), listed in the popup and exported as Markdown/HTML instruction sheets.",
  "type": "module",
  "scripts": {
    "probe": "node tests/probe.mjs",
    "zip": "node tools/zip.mjs",
    "gen-icons": "node tools/gen-icons.mjs"
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "puppeteer": "^25.7.0"
  }
}